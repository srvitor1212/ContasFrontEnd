

<EditForm Model="@entrada" OnSubmit="@Enviar">

    <div class="form-group">
        <label>Nome :</label>
        <div>
            <InputText @bind-Value="entrada.Nome" class="form-control col-sm-3" />
            @*<ValidationMessage For="@(() => person.FirstName)" />*@
        </div>
    </div>
    <div class="form-group">
        <label>Valor :</label>
        <div>
            <InputNumber @bind-Value="entrada.Valor" class="form-control col-sm-3" />
            @*<ValidationMessage For="@(() => person.FirstName)" />*@
        </div>
    </div>
    <div class="form-group">
        <label>Recebo até :</label>
        <div>
            <InputDate @bind-Value="entrada.DataVigencia" class="form-control col-sm-3" />
            @*<ValidationMessage For="@(() => person.FirstName)" />*@
        </div>
    </div>


</EditForm>

@code {
    Entrada entrada = new Entrada();

    private void Enviar()
    {
        
    }
}